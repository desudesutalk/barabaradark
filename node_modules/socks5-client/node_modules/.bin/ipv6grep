#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ipv6/bin/ipv6grep.js" "$@"
  ret=$?
else 
  node  "$basedir/../ipv6/bin/ipv6grep.js" "$@"
  ret=$?
fi
exit $ret
